{
  "name": "pinv-root",
  "private": true,
  "scripts": {
    "generate": "wagmi generate",
    "deploy": "cd contracts && ./deploy.sh",
    "deploy:base-sepolia": "cd contracts && ./deploy.sh sepolia",
    "deploy:base-mainnet": "cd contracts && ./deploy.sh mainnet"
  },
  "workspaces": [
    "web",
    "og",
    "contracts"
  ],
  "dependencies": {
    "viem": "^2.40.3",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "@wagmi/cli": "^2.8.0",
    "typescript": "^5"
  }
}